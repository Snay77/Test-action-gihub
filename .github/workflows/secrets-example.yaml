name: Utilisation des GitHub Secrets

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  secure-api-call:
    runs-on: ubuntu-latest
    steps:
      - name: Cloner le depot
        uses: actions/checkout@v4

      - name: Utiliser une clé API sécurisée
        run: echo "Ma cle API est:" ${{ secrets.API_KEY }}